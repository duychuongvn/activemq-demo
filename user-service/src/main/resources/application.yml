spring:
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
    non-blocking-redelivery: true
    packages:
      trust-all: true

server:
  port: 8081

logging:
  level:
    root: INFO

application:
  name: User Service